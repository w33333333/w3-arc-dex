# 验证记录

日期：2026-09-09。

- Solidity 0.8.30 编译成功；运行时代码 11,772 bytes，低于 EIP-170 限制。
- 16 项本地测试全部通过，无失败：包括支付、退款、账本守恒、伪造消息、重放/乱序、管理员提款限制、转账税、确认配置和前端字节码指纹。
- 测试请求/结果的接收调用耗气均低于配置的 250,000 gas 上限。此为本地模拟，不是主网 executor 成功交付保证。
- X Layer 与 BNB 的本地主网分叉都通过真实 Endpoint 上的本地部署、显式消息库配置、双 required DVN 配置和消息报价；详见 fork-test-results.json。
- 两个主网代币的 symbol、decimals、代码存在性及 Endpoint eid 已通过公开 RPC 核验；详见 token-check.json。
- Vite 生产构建成功。浏览器检查：未部署状态明确显示；方向切换正常；未安装钱包时给出提示；测试管理中包含部署、绑定、双验证器、1 W3 注资按钮；订单空状态正常；窄屏 DOM 无水平溢出。

本记录前述自动化验证在部署前完成。后续已按用户授权执行主网部署与配置；实际注资及暂停状态详见 mainnet-deployment.json。现已实测 0.01 W3 从 BNB 发出、X Layer 收到并领取；结果已回传，源链结算仍待确认。未完成双向往返测试；不构成合约安全审计。保留 X Layer 225,000 确认，本次观测约 62.5 小时，因此不承诺即时测试完成。
